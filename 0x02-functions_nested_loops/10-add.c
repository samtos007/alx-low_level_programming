#include "main.h"

/**
 * add - add two numbers from input
 * @m: first parametr
 * @n: second parameter
 *
 * Description: adds two numbers
 * Return: Always (0).
 */

int add(int m, int n)
{
	return (m + n);
}
