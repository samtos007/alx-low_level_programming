#include "main.h"

/**
 * _islower - this prints 1 or 0 depending on input
 * @i: frst parameter
 *
 * Description: prints all lowercase letters
 * Return: Always(0).
 */

int _islower(int i)
{
	return (i >= 'a' && i <= 'z');
}
