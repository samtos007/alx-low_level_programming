#include "main.h"

/**
 * _islower - the program prints 1 or 0 depending on input
 * @c: frst parameter
 *
 * Description: prints all lowercase letters
 * Return: Always(0).
 */

int _islower(int i)
{
	return (i >= 'a' && i <= 'z');
}
